# 元引擎 · 更新日志

## v1.0.5 (2026-08-14)

### 🔧 引用格式统一（本次主线）

- **正文引用统一「来源N」**：所有正文引用源标注统一为「来源1」「来源2」这一种格式，彻底消除 `（来源[N]）`、`来源[N]`、`[N]`、`【来源N】`、「来源：XXX」等混乱形态。前端清洗链重写为 9 步归一化 + N 值校验（引用编号超出参考链接范围时自动删除）。
- **来源链接可点击**：修复 `openExternal` 未挂载到 window 导致来源面板链接无法跳转的 bug；链接改为真实 `href` + `target=_blank`（浏览器原生跳转最可靠），Tauri 环境走系统 shell 打开。
- **来源数量优化**：参考链接上限从固定 5 条放宽到最多 8 条（3-8 自然浮动），URL 抽取兼容 Markdown 链接 `[text](url)` 格式。

### 🔧 防「AI 自说自话」

- **事实核对防自释**：新增 `isCleanCorrection` 强校验——事实核对模块对「修正文本」做校验，超 30 字 / 含 ⚠️ / 含「来源N」/ 含"关于·不符·需要核实·此处"等元描述词的不干净修正直接删除该事实，不再把 LLM 的解释性长句塞进正文。
- **LLM 自释括号清理**：新增清洗规则，捕获并删除 LLM 在正文中自带的解释性括号段（含 ⚠️、需注意、关于、由…发展 等元描述痕迹）。
- **推演标记清理**：（推演）（推理）（推测）纳入不确定关键词，正文不再残留这些标记。

### 📝 提示词优化

- 主 prompt 新增「引用格式全局硬规则」：明令唯一格式「来源N」+ 禁止清单，从源头统一引用输出。
- 事实核查裁判 prompt 收紧：明令修正文本 ≤30 字、禁止括号注释 / 警示符号 / 来源引用 / 元描述词。

### ⚠️ 决策记录

- 免费试用 3 次方案正式否决（Cloudflare Worker 到 DeepSeek 国内 API 的跨境链路延迟无解），转为强化配 Key 体验（一键获取 + 验证 Key）。

---

## v1.0.4 (2026-08-10)

### 🔧 Bug 修复 / 基础设施

- 引用来源清洗：清除来源编号残留的各种冗余标签，覆盖中文 `，、` 分隔符
- 应用图标改用 Cinzel 字体，与 UI logo 对齐
- 新增多平台 Release 工作流（GitHub Actions 自动构建 macOS arm64 / x86_64、Linux、Windows）

---

## v1.0.3 (2026-08-03)

### 🔧 Bug 修复

- **validateInput 中文正则修复**：修正 `[d\W_]+` 误判中文为特殊字符的 bug，改为 Unicode 属性匹配 `[\p{L}\p{N}]`。纯中文概念（"博弈论""贝叶斯定理"等）不再被错误拦截，修复前 startGenerate 提前 return 导致"生成无反应"。
- **401 认证错误显式抛出**：DeepSeek 搜索返回 401 时抛 `DEEPSEEK_AUTH_FAILED` 错误（`isAuthError: true`），不再静默降级到 wiki 管道，用户可明确知道是 Key 失效。
- **层级解析正则扩展**：parseMarkdown 支持更宽泛的层级分隔符（`·` `-` `—` 等），提升 markdown 解析兼容性。
- **GitHub 描述校准**：README/About 防幻觉措辞从"交叉验证+P0分级"校准到 L1 概念级校验实际能力，消除"描述>实际"的信誉风险。
- **版本对齐**：线上线下统一到 v1.0.3，本地 元引擎.app 从 1.0.0 升至一致基线。

---

## v1.0.2 (2026-08-03)

### 🔧 Bug 修复 / 一致性

- **进度条离散停靠**：生成管道进度条改为按「已彻底完成（生成 + 核对都完成）的层」离散推进；某一层尚未完成时进度条停在上一位等待，不再提前滑向下一层，消除「进度条已到第三层、但第三层还没生成完」的误判。文字实时显示「生成第 N 层… / 核对第 N 层…」作为等待期反馈。
- **输入校验加固**：`validateInput` 拦截空输入、超长（>120 字）、纯数字 / 纯符号、单字母及提示词注入（连续 3+ 换行或 ignore/override/system prompt 等关键词），避免无效 LLM 调用。
- **文档与代码对齐**：README / CHANGELOG 以 **DeepSeek + 12 模块**管线为最新标准版本，与 GitHub 保持一致；清理 v20 实验分支造成的源码 / 产物漂移。

---

## v1.0.1 (2026-08-02)

### 🔧 Bug 修复

- **单 Key 引导**：合并 API Key 和设置面板，首次打开引导页输入一个 Key 即可开始使用
- **修复 callLLMStream 参数顺序**：onChunk 和 systemPrompt 参数反转导致流式生成不触发
- **修复 module3Rule 未定义**：buildSystemPrompt 中引用未声明变量导致所有生成请求报错
- **修复 getSettings 默认值**：引导页只存 apiKey 时 apiBase 为 undefined，导致 fetch 失败
- **来源验证改为参考链接**：去掉 Wikidata/SearXNG 等渠道验证，直接展示搜索返回的参考链接列表
- **词条导出修复**：词条 .md 文件现在包含完整框架内容，而非仅一行定义
- **PDF/PNG 导出文字加深**：card-desc、miscon-insight-content 等元素从 muted 改为 ink 色
- **Safari 下载提示**：明确告知用户文件只能下载到「下载」文件夹
- **移动端优化**：viewport 禁止缩放，防止页面被意外放大
- **替换赞助二维码**：使用正确裁剪的微信赞赏码和支付宝收款码

---

## v1.0.0 (2026-08-02)

### 🚀 核心突破：告别 AI 幻觉

- **DeepSeek 联网搜索**：替代旧版四级维基管道（中文维基/英文维基/SearXNG/语义拆解），在国内直连可用，搜索成本约 ¥0.001/次
- **材料交叉验证**：搜索结果先经 DeepSeek 事实核查再评分，用可信度分数替代简单的字数/URL 计数，准确拦截部分搜索结果中的虚假内容
- **P0/P1/P2 防编造分级**：12 个模块中 4 个 P0 模块（发展脉络/底层原理/最新前沿/现实映射）绝对禁止编造，素材不足时跳过或标注
- **自适应模块生成**：根据搜索结果可信度动态调整生成模块数量和深度

### 📊 测试数据

- **50 词压力测试**：学术词 rich 率 88%（平均可信度 84.4%），随机词 68% medium
- **交叉验证拦截**：成功抓出 "咬文嚼字假榜单""虚构哈佛教授 O'Carroll""不存在的 2012 武汉虐猫事件" 等虚假内容
- **零编造生成**：管道在素材不足时诚实标注，不再出现 "MIT/北大/谷歌联合研究" 式编造

### 🔧 改进

- 首次打开引导页输入 API Key
- 生成时标注 ⚠️ 推演内容和不可靠来源

---

## v24 (2026-Q2) — 旧管线（已弃用）

- 四级搜索降级管道：中文维基 → 英文维基 → SearXNG → 语义拆解
- 12 模块认知框架生成
- Obsidian vault 自动同步
- 问题：公共 SearXNG 实例全部被封，维基 API 不稳定，LLM 在无素材时大量编造

---

## 版本语义

- 主版本号 → 重大架构变更或管道重写
- 次版本号 → 功能新增
- 修订号 → Bug 修复
