# 交付报告：变更单-20~24 批量恢复 + 变更单-24 首次执行

**时间**：2026-07-28

**背景**：index.html 工作区被回退到旧版本（1221行），所有变更单-20~24、脑图SVG、时间轴SVG、误区布局、SearXNG池、框架内搜索等全部丢失。

## 已恢复内容

### 变更单-20：generateSub genMode修复
- `generateSub` 入口备份 `prevMode = s.genMode`，finally 中恢复
- 防止子框架生成后全局 genMode 被静默改为 'cache'

### 变更单-21：LLM格式校验
- `validateStructure` 函数：检查层级/模块/内容长度
- 两处调用：主生成入口 + generateSub
- 校验失败时追加警告模块，不阻断渲染

### 变更单-22：SearXNG实例池
- 实例列表外置到 localStorage (`searxng_instances`)
- `trySearXNG` 函数：4s 超时 + 60s 冷却
- DDG 兜底保留并标记 deprecated

### 变更单-23：框架内搜索
- 搜索面板 UI（🔍按钮 + Ctrl+F 唤起）
- `performReaderSearch`：TreeWalker 遍历文本节点
- `jumpToMatch`：跨层跳转 + 高亮
- Enter/Shift+Enter 上下匹配，Esc 关闭

### 变更单-24：导航按钮隐藏 + 标签改大标题（首次执行）
- 导航按钮默认隐藏（opacity:0, translateY:12px），点击页面 toggle 显示
- 3秒自动隐藏
- 切换层时隐藏按钮
- 顶部标签改为显示当前层 h2 标题

### 脑图SVG（领域分类）
- `generateMindmapSVG`：三级树形SVG，字号用 probe 读取 `.module-content` 实际渲染大小
- `parseMindmapCategories`：解析 LLM 输出的加粗列表格式
- 间距用 `s` 缩放，字号不参与缩放

### 时间轴SVG（发展脉络）
- `generateTimelineSVG`：竖向SVG时间轴，accent色日期 + ink色事件
- svgW 根据实际最长行动态计算
- svgH 包含最后事件多行文本高度
- 字号与卡片正文一致

### 常见误区
- `generateMisconHTML`：误区+正解左侧，洞察右侧 flex 布局
- 前缀 span 不换行，内容对齐

### CSS恢复
- 模块卡片样式、脑图/时间轴SVG容器
- 误区flex布局、时间轴校验面板
- 框架内搜索面板、导航按钮显隐动画
- 暗色主题适配

## 验收方式
1. 打开页面，搜索"贝叶斯定理"生成框架
2. 领域分类应显示SVG脑图（非纯文本）
3. 发展脉络应显示SVG时间轴
4. 常见误区应显示结构化flex布局
5. 点击页面空白区域，底部导航按钮弹出，3秒后消失
6. 顶部右上角显示当前层名（如"宏观流动性"）
7. Ctrl+F 唤起搜索框，输入关键词可跨层搜索