---
status: delivered
from: 小T (Trae)
to: 小马 (Marvis)
date: 2026-07-28
change_order: 11
---

# 变更单-11 交付报告：发展脉络改为交替时间轴

## 执行摘要

全部 3 项变更已执行完毕。发展脉络模块已从左侧竖线+圆点样式改为中间竖线、日期左右交替的时间轴布局。

## 执行内容

### 变更1：CSS — 替换时间轴样式 ✅

**位置**：`index.html`，第 247-261 行

- 删除旧的 `.module-timeline .module-content p/li` 样式（左侧竖线+圆点，15 行）
- 新增交替布局 CSS（55 行）：
  - `.tl-line`：中间竖线，`left: 50%` + `translateX(-50%)`
  - `.tl-item`：宽度 46%，左右交替排列
  - `.tl-item.left`：右对齐 + 右侧 padding；`.tl-item.right`：左对齐 + 左侧 padding
  - `.tl-item::after`：圆形节点标记，落在中线上（left/right: -8px）
  - `.tl-date`：主题色加粗；`.tl-event`：正文色

### 变更2：新增 `generateTimelineHTML` 函数 ✅

**位置**：`index.html`，`escXml` 函数之后、`renderResult` 之前（约第 867 行）

- 解析 LLM 输出的时间轴文本（按行分割）
- 支持格式：`1763年：贝叶斯定理发表` / `1763年 - 贝叶斯定理发表`
- 去除 markdown 前缀（`-` / `*` / `1.`）
- 奇偶行交替分配到 left/right
- 生成 HTML：`<div class="tl-line">` + 多个 `<div class="tl-item">`
- 无法解析的行降级为纯文本事件显示
- **调整**：变更单中使用的 `esc()` 已统一改为项目已有的 `escXml()`

### 变更3：替换发展脉络渲染逻辑 ✅

**位置**：`index.html`，约第 1010 行

- 原逻辑：`content = marked.parse(mod.content || '')`
- 新逻辑：`content = generateTimelineHTML(mod.content || '')`

## 验收检查

| 验收条件 | 状态 |
|----------|------|
| 中间一条竖线 | ✅ `.tl-line` left:50% |
| 日期节点左右交替 | ✅ i%2 === 0 → left, 否则 right |
| 左侧日期右对齐、右侧日期左对齐 | ✅ text-align + padding |
| 每个节点有圆形标记落在中线上 | ✅ `::after` absolute positioning |
| 移动端正常显示 | ✅ 宽度 46% 自适应 |
