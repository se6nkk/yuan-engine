# 交付报告：变更单-56

**Commit**：`24626f1`

## 变更内容

行 2028，pill span 从标题文字后面移到前面：

```diff
- <div class="card-title">${esc(it.name)}<span class="card-pill ${statusClass}">${esc(it.status)}</span></div>
+ <div class="card-title"><span class="card-pill ${statusClass}">${esc(it.status)}</span>${esc(it.name)}</div>
```

## 验收
核心难题层卡片：状态胶囊在标题文字左侧，如"未解决 内容真实性验证"。
