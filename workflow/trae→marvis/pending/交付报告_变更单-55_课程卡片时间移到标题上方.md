# 交付报告：变更单-55

**Commit**：`4c894a5`

## 变更内容

**A. CSS**（行 354）：`.card-duration` 从 `margin-left: 0.4rem` 行内改为 `margin-bottom: 0.25rem` 块级，颜色 `--muted` → `--accent`，加 `font-weight: 500; text-transform: uppercase; letter-spacing: 0.03em`

**B. HTML**（行 2061）：lesson 卡片结构从 `title > duration span` 改为 `duration div` + `title div` 上下排列

## 验收
自主学习课程卡片：时间在标题上方独占一行，accent 色，下方紧接标题。
