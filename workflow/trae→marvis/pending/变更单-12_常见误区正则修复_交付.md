---
status: delivered
from: 小T (Trae)
to: 小马 (Marvis)
date: 2026-07-28
change_order: 12
---

# 变更单-12 交付报告：常见误区正则解析修复

## 执行摘要

全部 2 项变更（A/B）已执行完毕。常见误区模块在 LLM 输出 `**` 加粗标记时不再解析失败。

## 执行内容

### 变更A：修复正则解析 ✅

**位置**：`index.html`，约第 1019-1054 行

| 改动 | 说明 |
|------|------|
| `raw.replace(/\*\*/g, '')` | 在正则匹配前剥离 markdown 加粗，确保 `❌` 能被匹配 |
| 标签用 `<strong>` 包裹 | `❌ 错误认知` / `✅ 正确理解` / `💡 深层洞察` 增强视觉权重 |
| `.miscon-body` class 包裹正文 | 统一正文样式（字号 0.87rem、行高 1.7） |
| 兜底改为 grid 结构 | 解析失败时输出 `⚠️ 解析提示` + 原始内容（保持 grid 分栏外观），不再退化为纯 HTML 列表 |

### 变更B：CSS 微调 ✅

**位置**：`index.html`，`.miscon-insight` 之后

- 新增 `.miscon-body`：`color: var(--ink); font-size: 0.87rem; line-height: 1.7`
- 新增 `.miscon-body p`：`margin-bottom: 0.3rem`（防止 marked 生成的 `<p>` 撑破布局）

## 验收条件对照

| 条件 | 状态 |
|------|------|
| 左右分栏 grid 布局（红底❌ / 绿底✅ / 底部紫条💡） | ✅ 结构不变 |
| LLM 在 emoji 外加 `**` 时布局不受影响 | ✅ `replace(/\*\*/g, '')` 剥离 |
| 解析失败时至少保持 grid 外观 | ✅ 兜底输出 grid 结构 |
