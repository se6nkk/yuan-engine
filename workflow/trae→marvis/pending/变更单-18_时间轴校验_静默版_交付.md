---
status: delivered
from: 小T (Trae)
to: 小马 (Marvis)
date: 2026-07-28
change_order: 18
supersedes: 变更单-18_时间轴per-item校验+风险提示_交付.md
---

# 变更单-18：时间轴校验标记（静默版）— 交付报告

## 执行状态：已完成

## 变更清单

| # | 变更内容 | 状态 |
|---|---------|------|
| 1 | 渲染分支 — 去掉 ⚠ 警告横幅和 ⏳ 占位文案，改为空 div | ✅ |
| 2 | verifyAllTimelines — 替换为折叠式 `<details>`，不再操作 SVG | ✅ |
| 3 | 删除 renderTimelineVerifyResult 函数 | ✅ |
| 4 | CSS — 折叠参考面板（已在上一轮就位，本轮无改动） | ✅ |
| 5 | 清理旧版 CSS 残留（.tl-v-list 等）— 确认无残留 | ✅ |

## 与原始版的差异

| 维度 | 原始版（已回滚） | 静默版（当前） |
|------|-----------------|---------------|
| 顶部警告条 | ⚠ 橙色横幅 | 无 |
| 占位文案 | ⏳ 事件校验中… | 空（无文案） |
| SVG 操作 | 圆点变色 + ⚠ 文字标记 | 完全不动 |
| 下方展示 | 逐条列表 + 图例 | 折叠式 details，默认收起 |
| 断网行为 | 警告条仍在 | 无任何校验元素 |

## 交互流程

1. 用户搜索概念 → 进入阅读器 → 时间轴模块
2. **SVG 时间轴**：正常渲染，无任何额外标记
3. **异步校验**：Wikidata 返回后，SVG 下方出现灰色小字 `校验参考（N 项待确认）`
4. **点击展开**：每行显示 `✓ 年份 · 事件 Wikidata` 或 `⚠ 年份 · 事件 未在 Wikidata 中找到对应记录`
5. **全部通过**：显示 `校验参考（全部通过）`
6. **断网**：不出现任何校验元素，SVG 保持原样

## 验收对照

| 验收条件 | 结果 |
|----------|------|
| 时间轴 SVG 完全不变 | ✅ |
| SVG 下方出现灰色小字，可点击展开 | ✅ |
| 展开后每行显示校验状态 + Wikidata 链接 | ✅ |
| 全部通过时显示「全部通过」 | ✅ |
| 断网不出现任何校验元素 | ✅ |
