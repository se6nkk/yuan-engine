# 交付报告：变更单-48 · 修复四个 UI 缺陷

**交付人**：小T (Trae)
**交付时间**：2026-07-29
**分支**：feature/layout-pagination
**commit**：`cda8c31`

---

## 问题概述

1. **层数指示器显示"3/3层"而非"3/5层"** — 渐进渲染完成后 label 未正确刷新
2. **移动端核心概念索引空白** — 放射图 SVG 被 `display:none` 隐藏，且降级列表因 glossary 为空而无内容
3. **核心概念索引页搜索按钮冗余** — 用户明确要求删除
4. **网页端放射图分支不显示** — 渐进渲染早期 glossary 为空，放射图无节点；且卡片 pill 暗色模式下可读性差

---

## 变更内容

| 变更 | 内容 | 状态 |
|------|------|------|
| A | `appendRemainingLayers` 末尾改 `goToLayer(readerState.currentLayer)` 统一刷新 label/dots/nav | ✅ |
| B | 删除 `@media(max-width:640px)` 隐藏 SVG 规则，SVG 自然缩放；删除 `.radial-list` 降级列表 CSS | ✅ |
| C | `generateRadialDOM` 删除搜索按钮和降级列表 HTML | ✅ |
| D | `appendRemainingLayers` 末尾添加已渲染页放射图回填（使用完整 glossary） | ✅ |
| E | 暗色模式 pill 颜色增强：`active` → `#fbbf24`，`solved` → `#34d399`；`unsolved` 添加 border | ✅ |

---

## 关键逻辑

- **层数刷新**：`goToLayer` 统一处理 label、dots、nav 的更新，避免直接 textContent 赋值导致的显示不一致
- **放射图回填**：`appendRemainingLayers` 完成后，遍历所有 `.reader-page .module-radial`，用完整 glossary 重建放射图，解决早期渲染 glossary 为空的问题
- **移动端 SVG**：删除 `display:none` 规则，SVG 通过 `max-width:100%` 自然缩放

---

## 验收检查

- [x] JS 语法检查通过
- [x] git commit 已提交（`cda8c31`，+14/-20 行）
- [x] 变更单已归档至 `archived/`
- [x] pending 已清空