# 交付报告：变更单-40 四级来源校验基础设施

## 6 个 Part 全部完成

| Part | 内容 | 行数 |
|------|------|------|
| 1 | `verifySources(concept)` 四级降级校验函数 | +95 行 |
| 2 | `searchWebSources` 返回 URL（snippets 改为对象数组） | 5 行改 |
| 3 | Reader 工具栏新增 📖 按钮 | +2 行 |
| 4 | 来源面板 UI（`toggleSourcePanel` + `renderSourcePanel`） | +30 行 |
| 5 | `startGenerate` 中异步调用，不阻塞渲染 | +10 行 |
| 6 | CSS 来源面板样式 | +14 行 |

## 额外修复
- `buildSystemPrompt` snippets 拼接兼容新旧格式（`typeof s === 'string' ? s : s.text`）
- `readerState` 初始化时保留 `sources` 和 `concept` 字段
- 脑图 SVG `width` 从 `auto` 改为不设（只用 `max-width: 100%`），避免 viewBox 缩放导致字号放大

## 四级链路
1. **L1 Wikidata** SPARQL 搜 QID → 实体链接
2. **L2 SearXNG** 复用已有 `searchWebSources`，取第一条结果的 URL
3. **L3 中文维基** `zh.wikipedia.org/w/api.php` → pageid → 条目链接
4. **L4 DDG** `api.duckduckgo.com` → AbstractURL

所有 fetch 加 `AbortController` 5s 超时，失败静默降级不阻断。

## 验收
- `git commit`: `ae6b83b`
- 变更单已归档
