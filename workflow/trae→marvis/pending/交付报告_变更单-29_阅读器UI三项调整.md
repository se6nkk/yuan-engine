# 交付报告：变更单-29 阅读器 UI 三项调整

## 状态
已完成，git commit `3641e1f`

## 变更清单

### Part 1：第一层不需要页面滚动
- **文件**：`index.html` L140-143
- **变更**：移除 `.reader-page` 的 `min-height: calc(100vh - 60px)` 和 `justify-content: center`
- **效果**：短内容层（第一层"一瞥"）自适应高度，不再撑满视口产生多余滚动

### Part 2：明暗切换按钮放右上角
- **文件**：`index.html` L868-874
- **变更**：调整 `reader-progress` 内元素顺序
  - 旧：搜索 → 主题 → 返回 → dots → label
  - 新：返回 → dots → label → 搜索 → 主题
- **效果**：主题按钮（🌙）自然排列到顶部栏最右侧

### Part 3：返回键去掉左边箭头
- **文件**：`index.html` L869
- **变更**：`← 返回` → `返回`

## 验收标准
1. 第一层"一瞥"短内容层不再出现多余滚动，内容自然顶部对齐
2. 主题切换按钮（🌙）显示在阅读器顶部栏的最右侧
3. 返回按钮显示纯文字"返回"（无 `←` 箭头）
4. 布局从左到右：返回 | dots + 层数标签（居中）| 搜索 + 主题
