---
status: delivered
from: 小T (Trae)
to: 小马 (Marvis)
date: 2026-07-28
change_order: 13
---

# 变更单-13 交付报告：时间轴加宽 + 脑图对齐修复

## 执行摘要

全部 3 项变更（A/B/C）已执行完毕。时间轴日期列加宽，脑图文字对齐和连接线优化。

## 执行内容

### 变更A：时间轴加宽 ✅

| 属性 | 旧值 | 新值 |
|------|------|------|
| `.tl-line` left | 120px | 155px |
| `.tl-date` width | 100px | 130px |
| `.tl-date` padding-right | 1.6rem | 1.8rem |
| `.tl-date::after` right | -7px | -8px |

### 变更B：脑图连接线改为树形结构 ✅

- 新增垂直分支线：从一级标签右侧引出 `branchX = l1X + 16`，覆盖所有子项的 Y 范围
- 一级标签→垂直线：水平短线连接 `catCY` 到 `branchX`
- 垂直线→子项：每个子项从 `branchX` 水平短线连到 `l2X` 圆点
- 子项文字颜色从 `#333` 改为 `#444`（略提亮）

### 变更C：`dy=".35em"` 替代 `dominant-baseline="middle"` ✅

- 一级标签 `<text>` 和子项 `<text>` 均改用 `dy=".35em"` 居中
- 兼容性更好，跨浏览器一致

## 验收条件对照

| 条件 | 状态 |
|------|------|
| 长日期（如 `20世纪50年代`）完整显示在日期列内 | ✅ 130px 宽度 |
| 子项文字与圆点严格水平对齐 | ✅ dy=".35em" |
| 连接线：一级→垂直线→水平分支→子项 | ✅ branchX 树形结构 |
