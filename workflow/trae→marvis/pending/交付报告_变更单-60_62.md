# 交付报告：变更单-60 / 62

**Commit**：`01b05c0`

## 变更单-60：Obsidian 同步补充 glossary 数据

- `syncToObsidian` 函数签名增加 `glossary` 参数
- 两处调用点传入 glossary：earlyRendered 路径 + 兜底路径
- 同步服务 POST /save 接收 glossary 后写词条文件到 vault

## 变更单-62：核心概念索引卡片化

- 新增 `.glossary-cards` / `.glossary-card` / `.card-term` / `.card-desc` / `.card-jump` CSS
- `renderLayerPage` 中模块 10 渲染改为卡片网格：从 `data.glossary` 或 `extractGlossaryFromContent` 提取术语生成卡片
- 新增 `jumpToTerm(term, e)` 函数：点击"→ 深入探索"跳转首页搜索
- 术语 hover tooltip 保留（`showGlossary`）

## 验收
1. Obsidian vault 同时出现 `概念名-认知框架.md` 和词条文件
2. 核心概念索引显示为卡片网格，桌面端 4-5 列，手机端 3 列
3. 卡片悬停有上浮+阴影+边框高亮
4. 释义最多 2 行，点击跳转确认后搜索
