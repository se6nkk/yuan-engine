---
status: delivered
from: 小T (Trae)
to: 小马 (Marvis)
date: 2026-07-28
change_order: 16, 17
---

# 变更单-16 & 17：常见误区格式规约 + 时间轴格式规约 + Wikidata 校验 — 交付报告

## 执行状态：已完成

## 变更单-16：常见误区格式规约

| # | 变更内容 | 状态 |
|---|---------|------|
| 1 | System Prompt 强化误区格式（含双误区示例） | ✅ |
| 2 | 简化正则：跨行非贪婪 → 空行分块 + 单行匹配 | ✅ |

**正则变更要点**：
- 旧：`split(/(?:^|\n)(?=❌\s*错误认知)/m)` + `[\s\S]*?` 跨行非贪婪
- 新：`split(/\n\s*\n/)` 空行分块 + `(.+)` 单行匹配
- 去掉 3 个连锁前瞻断言，代码量减少约 30%

## 变更单-17：时间轴格式规约 + Wikidata 校验

| # | 变更内容 | 状态 |
|---|---------|------|
| 1 | System Prompt 强化时间轴格式（含示例） | ✅ |
| 2 | 渲染分支附加校验占位 div | ✅ |
| 3 | setTimeout 合并 verifyAllMindmaps + verifyAllTimelines | ✅ |
| 4 | 新增 4 个函数：verifyAllTimelines / parseTimelineEvents / queryTimelineEvents / renderTimelineVerifyResult | ✅ |
| 5 | CSS 校验提示样式（含暗色模式适配） | ✅ |

**时间轴校验机制**：
- API：Wikidata `wbsearchentities`（实体搜索，比 SPARQL 更宽容）
- 搜索词：`概念名 + 年份 + 事件描述`（截断 200 字符）
- 超时：5 秒总超时，超时后放弃剩余
- 断网/超时：静默隐藏，不报错

## 额外改进

- 暗色模式适配：变更单 17 未提及，已补充 `[data-theme="dark"]` 下的校验标签颜色
- 两个校验函数（mindmap + timeline）合并在同一个 `setTimeout` 中启动，不阻塞渲染
