# 交付报告：变更单-30 五层框架重构 + 四项 UI 修复

## 状态
已完成，git commit `737a25e`，净减 132 行（-209 +77）

## 变更清单

### Part 1：System Prompt 五层框架重写（最高优先级）
- **文件**：`index.html` `buildSystemPrompt` 函数
- **新五层结构**：
  ```
  一瞥:       1.核心定义
  全景地图:   2.领域分类  3.发展脉络  4.常见误区
  深层逻辑:   5.底层原理  6.最新前沿  7.核心难题
  拓展应用:   8.跨领域连接  9.现实映射  10.核心概念索引
  渐进学习:   11.推荐学习路径  12.自主学习课程  13.工具箱
  ```
- **废弃**：「误区与连接」「行动入口」「决策映射」「7B.首周可执行课表」
- 模块编号 1-13 连续，术语引用从「模块8」改为「模块10」

### Part 2：层名映射更新
- **文件**：`index.html` `parseMarkdown` 函数
- `layerNames`：`误区与连接`→`拓展应用`，`行动入口`→`渐进学习`
- `timeMap`：同步更新

### Part 3：术语表正则更新
- **文件**：`index.html` `extractGlossary` 函数
- 正则 `###\s*8\.\s*核心概念索引` → `###\s*10\.\s*核心概念索引`
- 变量名 `m8` → `m10`

### Part 4：发展脉络字体与正文对齐
- **文件**：`index.html` `generateTimelineSVG` 函数
- 移除探头计算（probe element）和 0.9×钳位逻辑
- `baseFs` 直接取 `bodyFs`（正文字号），时间轴字体=正文字号

### Part 5：脑图 SVG → CSS Flexbox DOM
- **删除**：`generateMindmapSVG`（~140行）、`makeRectTextBlock`（~18行）
- **新增**：`generateMindmapDOM` 函数 + `.mindmap` 系列 CSS（22行）
- **渲染调用**：`generateMindmapSVG` → `generateMindmapDOM`
- Flexbox 结构：root → branches(cat: title + items)
- 保留 `splitText` 和 `escXml`（时间轴仍在使用）

### Part 6：clearCache 添加用户反馈
- **文件**：`index.html` `clearCache` 函数
- 成功：`alert('缓存已清空')`
- 失败：`console.error` + `alert('清空失败，请刷新页面后重试')`

### 额外：CACHE_VERSION 升级
- `v2` → `v3`，所有旧缓存自动失效走重新生成

## 验收标准
1. 重新生成概念 → 5 层：「一瞥」「全景地图」「深层逻辑」「拓展应用」「渐进学习」
2. 模块编号 1-13 连续，无跳号乱序
3. 常见误区在第二层全景地图，❌→✅→💡 三段式
4. 发展脉络字体=正文字号
5. 领域分类渲染为 CSS Flexbox 思维导图（非 SVG）
6. 清空缓存有 alert 反馈
7. 不存在「误区与连接」「行动入口」「决策映射」旧名称

## 技术说明
- 旧 v2 缓存因版本号不匹配自动失效，用户无需手动清空
- `splitText` 保留（时间轴 `generateTimelineSVG` 仍在使用）
- `escXml` 保留（时间轴和思维导图 DOM 均使用）
- 净减 132 行代码，SVG 贝塞尔曲线复杂度完全消除
