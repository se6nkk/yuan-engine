---
status: delivered
task_from: wb
to: marvis
date: 2026-07-29
intent: 请小马(Marvis)据此写派工单给小T(Trae)
subject: 设计系统定位调整 — NeuralKinetics 美学须贴合「元框架引擎」内容型页面（不得硬套落地页模板）
---

# 设计决策交付：设计系统贴合内容型页面

## 背景
用户明确指示：**「设计系统要根据我们的页面内容做调整，不能完全硬套模板。」**
此前我交付的 NeuralKinetics 设计系统（2026-07-28 给小T 的 `neuralkinetics.css`/demo）与延展规划（07-28），**最初是纯按 NeuralKinetics 落地页写的**。但元框架引擎是**内容密集的阅读器**（五层渐进认知框架、长文 + 结构化可视化模块），与落地页（极少文字、超大留白、单一 Hero）气质不同，整页套用会破坏可读性。

本次交付：**把 NeuralKinetics 重新定位为「元框架引擎 reader 的视觉皮肤基底」**，并给出收敛清单。请你（小马）据此起草**派工单**发往 `marvis→trae/pending/`，由小T 执行。

## 本次随附资产（本目录内）
1. `NeuralKinetics设计系统_贴合内容型页面定位说明.md` — **核心文档**：气质差异对比、该复用/不该套清单、5 条收敛调整、给小T 的明确指令、验收点。
2. `NeuralKinetics多页面风格延展规划.md` — 已更新，新增 **§〇.5 强制约束**（须优先于原落地页式 §一~§四 执行）。
3. `neuralkinetics.css` / `neuralkinetics-demo.html` — 设计系统令牌与通用件**来源参考**（demo 是落地页原型，**仅用于看组件长相，禁止照搬布局**）。

---

## 📋 派工单草稿 A（请小马正式落成 `marvis→trae/pending/`）

**标题**：设计系统定位调整 — reader 视觉皮肤化（非整页替换）

**目标**：将 NeuralKinetics 美学作为「视觉皮肤层」叠加到现有 `index.html` 的 reader 上，而非整页替换。

**具体要求**：
1. 引入 NeuralKinetics 双主题令牌，**并入** reader 现有 CSS 变量（`--ink`/`--rule` 等），不另起一套系统；保证系统亮/暗切换时整站（含已有脑图/时间轴/误区对照模块）自动翻转。
2. 在 `<body>` 加 `.blob-wrap` 作**氛围背景**：透明度降到 `.30~.40`、位置固定安静、不再做落地页式页面变体；长文阅读区（`.module-content` 等）叠极淡 `--bg` 遮罩保可读性。
3. 通用件统一：pill、nav、footer、section-header、soft panel 改用 NeuralKinetics 样式。
4. **保留并强化**五层分页、长文渲染、领域分类脑图 / 发展脉络时间轴 / 误区对照等已有结构化模块；仅将它们的配色改为引用双主题令牌。
5. **禁止**：引入 Features/Pricing/About/Contact 等落地页骨架；照搬 `neuralkinetics-demo.html` 布局；把 reader 正文基准从 `0.9rem` 改成落地页 15px。

**验收点**（同定位说明 §五）：
- 流体球为氛围背景，长文在任何球色位置都清晰；
- 正文仍 ≈0.9rem(14.4px)，未被 Display 大标题挤占；
- 五层分页/长文/已有可视化模块结构完整保留；
- 系统亮暗切换全站（含已有模块）自动翻转、暗调球清晰；
- 颜色全走变量，无硬编码新色。

**风险提示**：之前已给小T 的 `WB交付_设计系统_2026-07-28.md` 集成要求第 2-3 条是"整页落地"思路（基于落地页假设），**本次派工单须明确覆盖/纠正它**，避免小T 误把 reader 整页换成 landing 风格。

---

## 待确认（用户侧，可先按默认推进）
1. **流体球强度**：默认透明度 .30~.40、固定安静位置、阅读区加遮罩。用户是否接受，或希望更隐形/稍强氛围？（见规划 §五.6）
2. 其余收敛项（字号基准、留白反转、通用件复用范围）已按定位说明默认，可直接进派工单。

## 备注
- 此为**设计定位调整**，不涉及后三层可视化（那套已单独交付、走另一派工单）。
- 小马写派工单时，可把上面「派工单草稿 A」直接扩充为正式工单格式（含验收清单、预估文件改动点）。
