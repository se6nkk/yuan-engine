---
status: delivered
task_from: wb
intent: 请小马(WB→你)据此设计简报，分解并撰写派工单(写入 marvis→trae/pending/)
date: 2026-07-29
subject: 元框架引擎 · 第三/四/五层可视化升级设计交付
---

# WB 交付：后三层可视化设计方案（请小马写派工单）

## 一、交付背景

用户反馈：`index.html` 的**第三 / 四 / 五层（模块 5–13）**目前全部是平铺 Markdown 大段文字，容易阅读疲劳。第一、二层已经做了选择性可视化（领域分类→脑图、发展脉络→时间轴、常见误区→三段式结构化），效果很好；现在要把这套视觉语言**延伸**到后三层。

详细诊断、模块映射、技术接入点都写在方案文档里（见下方资源）。**本简报已替你起草好派工单分解（A/B/C 三刀），你只需据此正式落成 `marvis→trae/pending/` 的派工单即可。**

## 二、核心设计决策（已定，无需再议）

1. **复用现有视觉语言**，不另起设计系统——reader 已有主题切换（`toggleTheme`），新模块一律走 reader 现有 CSS 变量/类名。
2. **9 个模块 → 4 类策略**：
   - ① 卡片网格（`generateCardGridHTML`，4 种 variant）→ 模块 7 难题 / 9 现实映射 / 12 课程 / 13 工具箱
   - ② 阶梯流（`generateStepperHTML`）→ 模块 11 学习路径
   - ③ 关系网络（`generateNetworkDOM`，chain/radial）→ 模块 5 底层原理 / 8 跨领域连接
   - ④ 术语胶囊云（`generateTermCloudHTML`）→ 模块 10 核心概念索引（联动现有 glossary）；模块 6 最新前沿**直接复用** `generateTimelineHTML`
3. **窄屏降级**：网络图 / 术语云在 ≤640px 降级为列表，绝不溢出。
4. **双主题 + reduced-motion + marked.parse 兜底**：沿用现有规范，LLM 输出异常时不白屏。

## 三、技术接入锚点（给小T 用，已核实行号）

- 分发逻辑：`renderResult()` 内 **约 975–1007 行** 的 `if/else if` 链，追加模块 5/6/7/8/9/10/11/12/13 分支
- 新增生成器挂在 **约 1673 行之后**（现有 `generateMisconHTML` 之后）
- ⚠️ 模块10 已有 glossary 包裹逻辑（**1010–1015 行**），改造时**必须保留**，不能破坏术语高亮
- 复用：`generateMindmapDOM` / `generateTimelineHTML` / `generateMisconHTML`

> 完整映射表、生成器签名、分发代码片段见方案文档第 3 / 4 / 5 节。

## 四、派工单草稿（请小马正式落地）

### 派工单 A — 卡片化 + 阶梯 + 时间轴复用（低风险·高收益）
- 新增 `generateCardGridHTML(raw, variant)`：`issue` / `scenario` / `lesson` / `tool` 四模板
- 新增 `generateStepperHTML(raw)`：四阶段阶梯，宽屏横 / 窄屏纵
- 模块 6 接入 `generateTimelineHTML`
- 分发接入 6 / 7 / 9 / 11 / 12 / 13
- **验收**：这 6 模块不再平铺；卡片网格响应式 1/2/3 列；双主题正常

### 派工单 B — 关系网络 + 术语云（中风险·新图生成器）
- 新增 `generateNetworkDOM(raw, mode)`：`chain` / `radial`，含窄屏降级
- 新增 `generateTermCloudHTML(raw)`：联动 glossary，点击触发解释
- 分发接入 5 / 8 / 10（保留 1010–1015 行 glossary 逻辑）
- **验收**：模块 5/8 网络图正确、窄屏降级；模块 10 胶囊云点击高亮

### 派工单 C — 定义 hero + 全局一致性（收尾）
- 模块 1 核心定义 → "定义 hero"（大字号 + 类比 callout）
- 全局入场动效统一、`prefers-reduced-motion`、间距节奏统一
- **验收**：第一层与后层视觉语言一致，无割裂

> 每张派工单交付后，由**小万(WB)** 跑解析/渲染核验并回测试报告（走 `wb→marvis`）。

## 五、随附资源（已在你 box 内）

| 文件 | 内容 |
|------|------|
| `元框架引擎_后三层可视化升级方案.md` | 完整设计依据（诊断 / 映射 / 生成器 / 接入 / 验收） |
| `neuralkinetics.css` + `neuralkinetics-demo.html` | 双主题 / pill / soft panel 视觉规范参考 |
| `mindmap-svg.zip` | 脑图 / 时间轴 / 误区对照模板与配色 |
| `误区对照教程.md` | `foreignObject` 自然换行等实现技巧 |

## 六、小马请确认 / 决策点

1. 派工单粒度是否同意 A/B/C 三刀，还是并成两张？
2. 模块 6 最新前沿是否直接复用时间轴（我建议复用，零成本），还是单独设计？
3. 是否需要把"模块 1 定义 hero"单独成一张派工单（目前在 C 阶段）？
4. 派工单发出后，验收是否仍由 WB 负责（按现有"四层分页阅读流验收"模式）？

---
*交付方：小万(WB) · 通道：wb→marvis/pending/ · 协议：Markdown + YAML(status/task_from)*
